# Portainer 的 --trusted-origins 须为 scheme://host 形式，
# 缺少 scheme 会导致启动失败：invalid trusted origin
- op: replace
  path: /spec/template/spec/containers/0/args/1
  value: "https://portainer.yourdomain.com"
