apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# base 只描述结构，不含任何环境相关取值。
# namespace、S3 桶名、集群名等由 overlays/<env>/ 注入。
resources:
  - logging-namespace.yaml
  - loki-config.yaml
  - loki-deployment.yaml
  - loki-ingress.yaml
  - alloy-config.yaml
  - alloy-daemonset.yaml
