apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# base 只描述结构与环境无关配置。
# namespace、域名、区域、ECR 账号由 overlays/<env>/ 注入。
resources:
  - auto-draw-io-configmap.yaml
  - auto-draw-io-deployment.yaml
