apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# base 只描述结构。namespace 由 overlays/<env>/ 注入。
resources:
  - convertx-configmap.yaml
  - convertx-ebs-pvc.yaml
  - convertx-deployment.yaml
